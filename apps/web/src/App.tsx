/**
 * Minimal placeholder shell. The real upload + form + results UI is built in
 * later tasks (Task 7 onward); this only needs to compile and mount.
 */
export function App() {
  return (
    <main>
      <h1>StickToThePlan</h1>
      <p>Vatternrundan race-plan calculator. The interface is under construction.</p>
    </main>
  );
}
