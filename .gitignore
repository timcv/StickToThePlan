# dependencies
node_modules/
# this repo uses npm (package-lock.json); ignore stray pnpm lockfiles
pnpm-lock.yaml
pnpm-workspace.yaml
# personal input data (GPX course, FIT rides) - not committed
data/
*.fit
*.gpx
# synthetic example route is safe to commit despite the *.gpx rule
!examples/sample-route.gpx
# bundled default route prefilled into the web form
!examples/vattern-315.gpx
# build / cache
dist/
.cache/
weather-cache/
# env
.env
.env.*
# os
.DS_Store
# generated output (keep the directory)
output/*
!output/.gitkeep
# connect iq
ciq/bin/
ciq/developer_key.der
*.prg
# Connect IQ data field source is now static and committed (ciq/source/*.mc)
.vercel
